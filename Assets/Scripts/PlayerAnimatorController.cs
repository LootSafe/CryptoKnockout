using UnityEngine;

public class PlayerAnimatorController : MonoBehaviour {

	enum ANIMATION_STATE { GROUNDED, NOTGROUNDED};
    enum CURRENT_ANIMATION { IDLE, JUMP, HURT, DEAD, HIGHPUNCH, LOWPUNCH, HIGHKICK, LOWKICK, BLOCK, SPECIALATTACKONE};

    // Animation logic will be handled here
}
